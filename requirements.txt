# Наша ИИ обучается на CPU. Всё, что нужно:
torch>=2.1          # библиотека тензоров/автоград (модель — наша, в model.py)
numpy>=1.24         # хранение токенов, батчи
tokenizers>=0.15    # алгоритм BPE (словарь учим сами на нашем корпусе)
datasets>=2.16      # потоковое скачивание публичных русских датасетов
tqdm>=4.66
